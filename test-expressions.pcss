div:
	// We expect the result to be 30
	width: 10 + 10 * 80 / 40
	// We expect the result to be 40
	width: (10 + 10) * 80 / 40 
	// We expect the result to be 80
	width: (10 + 10) * 80 / (40 - 20) 
	width: 10em * 0.5px
